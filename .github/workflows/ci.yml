name: my first pipeline

on: push

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v6
            - name: Build image
              run: docker build -t femiodedina/ciportfolio .
            - name: List images
              run: docker images
    